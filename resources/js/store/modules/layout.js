import { TOGGLE_MODAL } from "../mutations/layout";

const state = {
  modalVisible: false,
  modalComponent: "ProductItemModal"
};

const mutations = {
  TOGGLE_MODAL
};

const actions = {};

export default {
  state,
  actions,
  mutations
};
